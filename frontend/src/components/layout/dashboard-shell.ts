/** Padding horizontal partagé header + contenu dashboard */
export const dashboardShellPadding =
  "px-3 pb-[max(1.25rem,env(safe-area-inset-bottom))] sm:px-5 lg:px-6";

/** Conteneur pleine largeur — header + contenu dashboard */
export const dashboardShellContainer = "w-full min-w-0";
